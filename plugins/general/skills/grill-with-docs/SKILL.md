---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADRs and glossary) as we go.
disable-model-invocation: true
---

Run a `/general:grilling` session, using the `/plan:domain-modeling` skill if it is
installed. Service and Support does not ship `plan`; there, grill without it —
the documents are an enrichment, not a prerequisite.
